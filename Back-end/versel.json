{
  "version": 2,
  "builds": [
    {
      "src": "index.js",
      "use": "@versel/node",
      "config": {
        "environment": {
          "PORT": 3030,
          "MONGO_URI": "${MONGO_URI}"
        }
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}
